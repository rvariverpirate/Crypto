#include <iostream>
#include <algorithm>
#include <bits/stdc++.h> 
using namespace std;


int main(){
    // Values to insert in our 
    string values[8] = {"POT", "PIE", "CAT", "COW", "RAT", "OWL", "YAK", "   "};
    int n = sizeof(values)/sizeof(values[0]);
    sort(values, values+n);

    

    return 0;
}